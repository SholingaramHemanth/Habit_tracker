import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aurahabittracker.app',
  appName: 'Aura Habit Tracker',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;